defaults:
  - launcher: local
  - testing: corpora2.yaml
  - _self_

output_dir: ???

launcher:
  partition: ??? # set as null if running locally
  cache:
    caching_dir: ${output_dir}/cache  # Cache won't be re-used if you change the output_dir
 
corpora:  ???                                                                                                                                                                                                                                                                                                                                                    
